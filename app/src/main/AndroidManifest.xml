<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application android:allowBackup="true" android:icon="@mipmap/turtle_viewer_icon" android:label="Turtle Viewer" android:theme="@style/Theme.TurtleViewer" android:supportsRtl="true">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="text/turtle" />
                <data android:mimeType="application/x-turtle" />
                <data android:mimeType="application/turtle" />
                <data android:mimeType="application/gpx+xml" />
                <data android:mimeType="application/gpx" />
                <!-- Several file managers label GPX documents as generic XML. -->
                <data android:mimeType="application/xml" />
                <data android:mimeType="text/xml" />
                <data android:mimeType="application/json" />
                <data android:mimeType="text/json" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/turtle" />
                <data android:mimeType="application/x-turtle" />
                <data android:mimeType="application/turtle" />
                <data android:mimeType="application/gpx+xml" />
                <data android:mimeType="application/gpx" />
                <data android:mimeType="application/xml" />
                <data android:mimeType="text/xml" />
                <data android:mimeType="application/json" />
                <data android:mimeType="text/json" />
            </intent-filter>
            <!-- Restricts the MIME-unknown fallback to supported filename extensions. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:host="*" android:pathPattern=".*\.ttl" />
                <data android:scheme="content" android:host="*" android:pathPattern=".*\.gpx" />
                <data android:scheme="file" android:pathPattern=".*\.ttl" />
                <data android:scheme="file" android:pathPattern=".*\.gpx" />
                <data android:scheme="content" android:host="*" android:pathPattern=".*\.json" />
                <data android:scheme="file" android:pathPattern=".*\.json" />
            </intent-filter>
        </activity>
    </application>
</manifest>
